version: '3'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      DJANGO_SETTINGS_MODULE: ideathon_2024_webpage.settings
    command: >
      sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
